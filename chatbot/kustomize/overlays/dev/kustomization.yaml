apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

nameSuffix: "-dev"
commonLabels:
  app: chatbot-dev

patches:
  - path: deployment.yaml
    target:
      kind: Deployment
      name: chatbot-deployment